#pragma config(Motor,  port2,           FL,            tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port3,           FR,            tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port4,           BL,            tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port5,           BR,            tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port6,           CL,            tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port7,           CCL,           tmotorServoContinuousRotation, openLoop)
#pragma config(Motor,  port8,           UP,            tmotorServoContinuousRotation, openLoop)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//
/*
NOTES
LR//ports to wheels
23F
45B
LR//don't ask me why but these are the polarities:
++F
--B
p6+//pull
p7-//pull
p8-//up
Ch1 EAST+
Ch2 NORTH+
Ch3 NORTH+
Ch4 EAST+
*/
task main() {
  while (1 == 1){
    void lt(speed){//left group
      motor[port2] = speed;
      motor[port4] = -speed;
    }
    void rt(speed){//right group
      motor[port3] = speed;
      motor[port5] = -speed;
    }
    if (vexRT[Ch2] == 0){//point turns, positive is clockwise
      lt(vexRT[Ch1]);//east||clockwise
      rt(-vexRT[Ch1]);//west||clockwise
    }else {//differential
      lt(vexRT[Ch2] + vexRT[Ch1]);//forward plus east
      rt(vexRT[Ch2] - vexRT[Ch1]);//forward minus east
    }
    void pl(value){//pull
      motor[port6] = value;
      motor[port7] = -value;
    }
    if (vexRT[Btn6U] == 1){
      pl(127);
    }else if (vexRT[Btn6D] == 1{
      pl(-127);
    }else {
      pl(0);
    }
    //up
    if (vexRT[Btn5U] == 1){
      motor[port8] = -127;
    }else if (vexRT[Btn5D] == 1){
      motor[port8] = 127;
    }else {
      motor[port8] = 0;
    }
  }
}
